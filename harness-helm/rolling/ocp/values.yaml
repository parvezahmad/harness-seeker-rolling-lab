replicaCount: 2

image:
  repository: ghcr.io/parvezahmad/harness-seeker-demo
  tag: "1.0.100"
  pullPolicy: Always

environment: dev

healthStatus: healthy

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 256Mi
